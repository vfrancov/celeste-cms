import { Routes } from '@angular/router';
import { GraphicsPageComponent } from './graphics.component';

export const graphicsRoute: Routes = [
  { path: 'graphics', component: GraphicsPageComponent }
]
